services:
  web:
    build: .
    command: gunicorn controlroomproject.wsgi:application -c controlroomproject/gunicorn_settings.py
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    env_file:
      - .env
    # network_mode: host
